{
  "name": "globe-zones-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && tsc --noEmit -p tsconfig.node.json && vite build",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.node.json",
    "preview": "vite preview",
    "build:zones": "node scripts/build-zones.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "@types/node": "^22.10.1",
    "pngjs": "^7.0.0",
    "typescript": "~5.6.3",
    "vite": "^5.4.11"
  }
}
